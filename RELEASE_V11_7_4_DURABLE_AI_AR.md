# إصدار CampusPass IQ V11.7.4 — Durable AI Support

هذه النسخة مبنية مباشرة على ملف المشروع الذي رفعه المالك باسم `CampusPass-IQ-Bot-222.zip`، وليست مبنية على حزمة قديمة أو نموذج منفصل.

## التغيير الوظيفي

تم استبدال استدعاء Gemini المباشر داخل Handler الدعم بمسار دائم:

```text
سؤال المستخدم
→ تحقق FSM والصلاحيات والموافقة
→ تنقيح الأسرار
→ حفظ Job في PostgreSQL
→ رد فوري وإخلاء الـFSM
→ عامل داخلي في الخدمة المجمعة
→ سياق محدود من بيانات المستخدم المملوكة له
→ Gemini غير متزامن
→ تعديل رسالة الانتظار بالجواب
→ تذكرة بشرية عند الفشل أو عدم حل المشكلة
```

## ما لم يتم حذفه أو استبداله

لم تُحذف مسارات الطالب أو المزود أو المالك، ولم يُستبدل نظام OTP/IMAP، ولم تتغير جداول الدفع أو المحفظة أو الضمان أو التقارير أو باقة الأصدقاء أو المخزون. التغيير محصور في تكامل المساعد الذكي، إعداداته، وربطه بالتشغيل.

## Render Free

- مورد Web واحد فقط.
- `RUNTIME_MODE=combined`.
- لا Worker مدفوع منفصل.
- لا Redis إجباري.
- Queue Gemini محفوظة في PostgreSQL/Supabase.
- Migration الإصدار تعمل تلقائياً عند بدء التطبيق.

## التشغيل

أضف في Render Environment:

```env
FEATURE_GEMINI=true
GEMINI_API_KEY=YOUR_GOOGLE_AI_STUDIO_KEY
```

بقية الإعدادات الآمنة موجودة في `render.yaml` و`GEMINI_RENDER_VARIABLES.example`.
