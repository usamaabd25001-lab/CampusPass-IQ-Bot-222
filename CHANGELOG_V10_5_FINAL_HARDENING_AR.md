# CampusPass IQ V10.5 — التثبيت النهائي المترابط

## واجهة المستخدم والتنقل
- توحيد تحديث واجهات الأزرار الشفافة داخل الرسالة الحالية بواسطة `edit_or_send` مع قفل لكل رسالة لمنع سباقات الضغط.
- حذف الرسالة القديمة قبل أي بديل، وعدم إنشاء بديل إذا رفض تيليغرام حذف الأصل؛ وهذا يمنع ظهور واجهتين متجاورتين.
- إضافة تذييل رجوع وقائمة رئيسية بصورة idempotent إلى لوحات المفاتيح القابلة لإعادة الاستخدام وإلى مصفوفات لوحة المنصة والإدارة.
- معالجة الأزرار القديمة أو غير المعروفة في آخر Router دون مسح حالة FSM أو إنشاء رسالة جديدة.
- منع رسائل المعالجة المؤقتة من الظهور قبل تنفيذ Callback أو البقاء في سجل المحادثة.

## سرعة الـCallbacks والاستعلامات
- جميع دوال Callback المسجلة تبدأ حرفياً بـ `await callback.answer()` مرة واحدة فقط.
- مهلة قصيرة لتنظيف حالة التنقل في Redis، ومهلة محدودة لاستعلام الحظر الأمني، مع رد واضح بدلاً من تعليق الزر.
- تخطي طابور العمليات الثقيلة في Middleware للـCallbacks حتى يصل الإقرار إلى تيليغرام فوراً.
- استمرار حدود التزامن على مستوى Dispatcher والخدمات والمهام الخلفية.

## Payloads
- فحص مركزي لكل `callback_data` قبل الإرسال ورفض أي حمولة تتجاوز 64 بايت محلياً.
- استخدام مولدات Payload قصيرة في الإعلانات والمسارات الديناميكية، مع Fallback آمن للأزرار القديمة.

## FSM والتحقق من المدخلات
- تعريف حالات الصور فقط، الصور أو الرابط، الصور أو الملفات، الوسائط فقط، والوسائط أو النص بصورة صريحة.
- جميع حالات FSM الأخرى أصبحت نصية افتراضياً، مع رفض الفراغ والمرفقات الخاطئة والمحارف غير الصالحة والمدخلات المفرطة الطول.
- التنقل السريع لا يحول ضغط زر قديم إلى رسالة انتهاء وهمية ولا يمسح العملية الجديدة.

## العروض والمخزون والمهام الخلفية
- فحص دوري مفهرس للعروض المنتهية والمخزون المنتهي أو النافد.
- استرجاع السعر الطبيعي بعد انتهاء الخصم، وإخفاء العرض المنتهي أو النافد، وإعادة تفعيله عند توفر مخزون صالح.
- إشعار موظفي المنصة مرة واحدة لكل انتقال مهم بواسطة مفاتيح idempotency.
- استعلام دورة الحياة يقرأ العروض المرشحة فقط ويجمع مخزونها دفعة واحدة بدلاً من تحميل جميع العروض والحسابات.

## قاعدة البيانات والنشر
- إضافة فهارس `ix_cp_provider_active_status` و`ix_cp_offer_lifecycle` و`ix_cp_inventory_lifecycle` بترقية Alembic إضافية غير هدامة.
- إصلاح أداة التحقق لتعمل من Docker أو من مسار `scripts` بإضافة جذر المشروع إلى `sys.path`.
- الإصدار: `10.5.0-final-hardening`.
