# تقرير فحص CampusPass IQ v4.3.0

## تم التحقق محليًا

- [x] جميع اختبارات البوت الحالية: 33 اختبارًا.
- [x] Ruff lint وformat.
- [x] Compileall للمشروع وملفات العمليات.
- [x] Parsing لملفات GitHub Actions YAML.
- [x] تشغيل شاشة المساعدة لأداة اختبار التزامن من جذر المشروع.
- [x] عدم وجود قيم أسرار حقيقية داخل الحزمة.
- [x] اختبار تكاملي Mock لمسار إنشاء النسخة، تشفيرها، رفعها، تنزيلها، فكها وفحصها.
- [x] اختبار حاجز الأمان الذي يرفض اسم قاعدة إنتاج لا يحتوي load/test/staging/qa.

## يحتاج تنفيذًا بعد الرفع إلى GitHub

- [ ] تشغيل Backup يدوي أول مرة.
- [ ] فتح Release والتأكد من ظهور ملف `.tar.gz.gpg` وملف `.sha256`.
- [ ] تشغيل Restore بوضع `verify`.
- [ ] تشغيل اختبار 500 محاولة ومخزون واحد على قاعدة اختبار منفصلة.
- [ ] رفع الاختبار تدريجيًا إلى 1000 ثم 3000 محاولة حسب قدرة PostgreSQL.

## تحذير

`LOAD_TEST_DATABASE_URL` و`RESTORE_DATABASE_URL` يجب أن يكونا لقواعد منفصلة قابلة للمسح، وليس لقاعدة الإنتاج.
