# سجل القرارات الهندسية والتجارية

## D-001 — نموذج الأموال في الإطلاق الأول
الطالب يدفع للمنصة مباشرة، والبوت لا يحتفظ بأموال المستخدمين. السحب متوقف افتراضياً.

## D-002 — لا تجاوز للباقات بسبب دور Owner
مالك المنصة ليس مالك البوت. ميزات المنصة تخضع إلى باقتها، والتجاوز الوحيد لحسابات `ADMIN_IDS`.

## D-003 — قاعدة البيانات خارج الاستضافة
تغيير Railway لا يغير البيانات. يجب حفظ `DATABASE_URL` و`ENCRYPTION_KEY` خارج GitHub والمحادثات.

## D-004 — السحب يحتاج Marketplace حقيقياً
لا يمكن تفعيل السحب إلا بعد بوابة رسمية وتسوية قانونية وتقنية. النسخة الحالية تستخدم `provider_direct_prepaid_commission`.

## D-005 — التحديثات على دفعات
كل دفعة تملك Version وMigration وتقرير قبول ونسخة رجوع مستقلة.

## D-006 — بوابة Mastercard
تبقى متوقفة حتى وجود مزود فعلي، حساب تاجر، Sandbox، توقيع Webhook، استرجاع ومطابقة يومية.

## D-007 — الاسترجاع في نموذج الدفع المباشر
المنصة تحول المبلغ للمستخدم خارج البوت، ثم تسجل المرجع داخل البوت. لا يعكس البوت الحسابات ولا يغلق النزاع إلا بعد تأكيد المستخدم أو مدير البوت استلام التحويل.

## D-008 — اعتراض رسمي واحد لكل طلب
لكل طلب نزاع رسمي واحد يمنع التلاعب وإعادة فتح دورة مالية متكررة. بعد إغلاقه يمكن فتح تذكرة دعم استثنائية، وليس نزاعاً مالياً جديداً تلقائياً.

## D-009 — المورد المكشوف لا يعود للبيع تلقائياً
عند استرجاع كامل لخدمة سُلّمت، ينتقل المورد إلى `PROBLEM` وتُنشأ مهمة تدوير أو إتلاف. حتى بعد تأكيد التدوير لا يعود إلى `AVAILABLE` آلياً؛ يحتاج مراجعة مخزون مستقلة في دفعة لاحقة.

## D-010 — الأرباح صافية بعد الاسترجاع
تقارير الإدارة والمنصة تطرح قيود الاسترجاع المؤكدة. الاسترجاع الموعود أو غير المؤكد لا يغير الأرباح المحاسبية.

## D-011 — PII مشفر وMasked افتراضياً
الاسم والهاتف وبيانات التفعيل الكاملة لا تعرض داخل شاشات الإدارة العامة. الوصول الصريح يحتاج صلاحية ويسجل.

## D-012 — دليل واحد مسجل ومحدد المدة
إثباتات الدفع والدعم والنزاع والاسترجاع تمر عبر EvidenceAsset. تحفظ Telegram references مشفرة، ويُفعّل تخزين خارجي قبل الإطلاق المالي الواسع.

## D-013 — التصدير لا يعيد توزيع الأسرار
ملف تصدير المستخدم يحتوي بيانات الحساب والطلبات، لكنه يخفي كلمات المرور والرموز وOTP حتى لا يصبح الملف نفسه خطراً أمنياً.

## D-014 — الحذف هو إخفاء هوية منظم
تزال الهوية الشخصية بعد فترة تراجع وبعد انتهاء الطلبات النشطة، بينما تبقى القيود المالية الضرورية منفصلة عن الهوية لأغراض المحاسبة والنزاع.

## D-015 — التصفح قبل التسجيل الكامل
المستخدم يشاهد المنصات والعروض بعد قبول الشروط. لا نجمع الملف الدراسي الكامل إلا عند الحاجة، وأول شراء يطلب الحد الأدنى ثم يتيح الإكمال لاحقاً.

## D-016 — لا طلب قبل موافقة نهائية
جمع بيانات التفعيل لا يعني إنشاء الطلب. لا يحجز النظام مورداً ولا يبدأ مهلة الدفع إلا بعد شاشة مراجعة وضغط المستخدم على موافقة صريحة.

## D-017 — الاستلام مختلف عن نجاح التفعيل
`delivery_acknowledged_at` يثبت وصول البيانات، و`activation_confirmed_at` يثبت أن المستخدم جرّب الخدمة ونجحت. لا نخلط الحالتين في واجهة أو محاسبة.

## D-018 — أرقام عامة وصفحات وعرض عربي مركزي
البحث يستخدم Public IDs مع تحقق ملكية. السجلات الطويلة تعرض بصفحات، وقيم قاعدة البيانات الإنجليزية لا تظهر للمستخدم مباشرة بل تمر عبر طبقة العرض العربية.


## D-019 — قاعدة البيانات هي مصدر حالة النشر والمهام
لا نعتمد على ذاكرة العملية لمعرفة إن كان التقرير أو النسخ الاحتياطي نُفذ. كل تشغيل مهم يسجل في PostgreSQL بمفتاح فريد وLease.

## D-020 — النسخة الاحتياطية لا تعتبر ناجحة بمجرد الرفع
الحالة النهائية `verified` فقط بعد تنزيل الملف المشفر وفك تشفيره ومقارنة SHA-256.

## D-021 — Restore عملية خارج البوت
لا يوجد زر Telegram يستعيد قاعدة الإنتاج. الاستعادة تتم بأداة خارجية وتأكيد صريح وعلى قاعدة منفصلة أولاً.

## D-022 — تدوير المفتاح تدريجي
المفتاح الجديد يصبح Primary، والمفاتيح القديمة تبقى في Keyring للقراءة حتى إعادة تشفير جميع السجلات والتحقق من Backup جديد.

## D-023 — Staging لا يشارك Bot Token الإنتاج
بيئة الاختبار تستخدم Bot مستقل. النظام يرفض بصمة توكن الإنتاج عند تفعيل Staging guard.

## D-024 — فصل العامل اختياري
البداية تبقى `combined`. عند زيادة الحمل يمكن فصل `bot` و`worker` مع قاعدة وRedis ومفاتيح مشتركة، وخدمة bot واحدة فقط لكل Token.
