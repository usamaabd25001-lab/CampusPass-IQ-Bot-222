# CampusPass IQ V10 Render Ready

- إضافة `/ping` خفيف داخل خادم FastAPI الموجود، من دون تشغيل خادم ثانٍ أو تعارض منافذ.
- دعم Render وقراءة `PORT` وبيانات Render التلقائية.
- تحسين اتصال PostgreSQL/Supabase عبر SQLAlchemy async وasyncpg، مع دعم Supavisor transaction pooler وإيقاف prepared-statement cache عند المنفذ 6543.
- تنبيه Telegram للإدارة عند إغلاق/إعادة تشغيل البوت.
- تنبيه عاجل عند فشل أو انتهاء صلاحية Google Drive refresh token.
- محدد طلبات صارم: طلب واحد لكل مستخدم كل ثانيتين.
- رسالة معالجة فورية قبل عمليات IMAP والذكاء الاصطناعي والتقارير.
- IMAP غير حاجب باستخدام `asyncio.to_thread` مع مهلات واستعادة كاملة لحالة الطلب والبريد والاشتراك عند الفشل.
- تقارير HTML حديثة، رسمية، A4، مع اسم المزود الإنجليزي، إصلاح الشعار، وتدرج Free/Plus/Pro.
- دعم مباشر للطالب مع تحويل المشكلة إلى حساب Telegram الخاص بالمزود بدل فتح نزاع مالي جديد.
- لوحة المزود تعرض CV الطالب والاسم وتاريخ ووقت بداية ونهاية الاشتراك بدقة.
