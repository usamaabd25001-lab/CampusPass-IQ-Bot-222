# استعمل هذا الملف فقط عندما تكون PostgreSQL خدمة داخل نفس مشروع Railway.
# اسم خدمة قاعدة البيانات قد يكون Postgres أو اسمًا آخر؛ اختره من Add Reference Variable.

DATABASE_URL=${{Postgres.DATABASE_URL}}
REQUIRE_EXTERNAL_DATABASE=false
DB_SSL_MODE=disable
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10
DB_POOL_PRE_PING=false
DB_POOL_TIMEOUT_SECONDS=10
DB_POOL_RECYCLE_SECONDS=900
DB_PREPARED_STATEMENT_CACHE_SIZE=100
DB_APPLICATION_NAME=campuspass-iq-railway
