# إصلاح خطأ ImportError الخاص بقاعدة البيانات

هذه النسخة تستخدم الملف:

```text
app/database.py
```

بدل `app/db.py`، حتى لا يتعارض مع أي مجلد قديم اسمه `app/db/` في مستودع GitHub.

## الرفع الآمن

1. فك ضغط الملف.
2. ارفع محتويات المجلد إلى جذر المستودع.
3. تأكد أن الملف `app/database.py` موجود.
4. لا تعدّل سطر التشغيل؛ Railway سيستخدم `python -m app.main`.
5. أعد النشر من Railway.

يمكن إبقاء المجلد القديم `app/db/` مؤقتًا لأن الكود الجديد لا يستورده، لكن الأفضل حذفه من GitHub لاحقًا لتنظيف المستودع.
