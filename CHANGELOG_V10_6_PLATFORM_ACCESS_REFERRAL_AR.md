# سجل تغييرات CampusPass IQ V10.6

## الواجهات والتنقل

- أزيل زر «خصوصيتي 🔐» و«طلب حذف وإخفاء بياناتي» من إعدادات القوائم والـrouter والأوامر العامة.
- أضيفت دالتا `remove_reply_keyboard_temporarily()` و`install_reply_keyboard_temporarily()` لتبديل الكيبورد برسالة مؤقتة تُحذف فوراً.
- أُصلحت `delete_safely()` لضمان حذف الرسالة المؤقتة وعدم ترك أثر «⌨️» في المحادثة.
- الرجوع والرئيسية والإلغاء تمسح حالة Aiogram FSM مركزياً قبل متابعة المسار المقصود.

## صلاحيات المنصة

- أضيفت `normalize_telegram_user_id()` و`is_platform_authorized()` كمصدر صلاحية موحد.
- المقارنة تتم بعد تحويل Telegram ID إلى `int` وباستعلام JOIN صريح على المستخدم وموظف المنصة والمنصة الفعالة.
- أضيف حقل `cp_users.has_platform_access` وبوابة شروط لأول دخول.
- لوحة المنصة لا تظهر إلا للمستخدم المرتبط بمنصة فعالة؛ لوحة الإدارة لا تظهر إلا لـ`ADMIN_IDS`.

## الدعوات

- أوقف الإيداع المالي للإحالة نهائياً في التدفق الجديد.
- تُسجل الإحالة عند أول طلب مكتمل للمدعو فقط وبمفتاح idempotency.
- كل ثلاث إحالات ناجحة تصدر كوبون `FEE_WAIVER` مستهدفاً لصاحب الدعوة، استخدام واحد وحد مستخدم واحد.
- واجهة «نقاطي ودعواتي» تعرض التقدم الدوري والأكواد غير المستخدمة.

## الشعار

- مسار الشعار يقبل `message.photo` فقط ويحفظ `message.photo[-1].file_id`.
- لا توجد أي مكالمة Google Vision أو Gemini أو HTTP داخل `BrandingService`.
- روابط الصور غير مقبولة في هذه الخطوة لتجنب التنزيل الخارجي والمرفقات الخاطئة.

## قاعدة البيانات والبناء

- ترقية Alembic: `1060_platform_access_referral_cleanup` ورأس واحد فقط.
- ترقية داخلية إضافية غير هدامة: `10.6.0-platform-access-referral-cleanup`.
- Docker يشغّل فحص V10.5 الرجعي وفحص V10.6 الجديد قبل قبول الصورة.
