# قواعد الأمان — V4.1

- لا ترفع `.env` أو Tokens أو مفاتيح البريد والدفع إلى GitHub.
- لا تغيّر `ENCRYPTION_KEY` بعد وجود مخزون أو حسابات بريد مشفرة.
- صورة الدفع لا تؤكد العملية تلقائيًا؛ موظف مخول يراجعها.
- كل وسيلة دفع تُفحص Server-side ضد منصة الطلب.
- مراجع الدفع وGateway References فريدة.
- المخزون يُشفّر، وتُحفظ بصمة Hash مستقلة لمنع التكرار.
- أسرار IMAP وOAuth وApp Password لا تُرسل للطالب.
- رسالة البريد والرمز مرتبطان بحجز وطلب ووقت، ولا يُعاد استخدامهما.
- قبول الدفع والتسليم يستخدمان Idempotency وOutbox.
- كل إجراء إداري يتحقق من الصلاحية والمنصة في الخادم، وليس الزر فقط.
- لا يسمح برفع Python أو Shell أو Executables من Telegram.
- Webhooks تستخدم HMAC وسرًا منفصلًا.
- شغّل نسخة Polling واحدة فقط لكل Bot Token.
- افحص Backup Restore، وليس إنشاء Backup فقط.
- استخدم `MAINTENANCE_MODE` أثناء التحديثات الكبيرة.
- راقب مهام التسليم الفاشلة وحسابات البريد من Health Dashboard.


## متطلبات V4.1

- `API_ADMIN_TOKEN` مطلوب لحماية `/admin/health`.
- `/health` و`/health/ready` لا يعرضان تفاصيل قاعدة البيانات أو الوحدات.
- روابط التقارير محدودة بالعمر وعدد مرات الفتح وتُخزن بصمتها فقط.
- لا تفعّل Mastercard قبل مطابقة Adapter مع وثائق الجهة المتعاقدة.
- استخدم Redis في النشر متعدد النسخ، مع بقاء PostgreSQL هو مصدر الحقيقة.
- شغّل `python scripts/generate_secrets.py` للنشر الجديد، ولا تغير مفتاح التشفير القديم أثناء الترقية.
