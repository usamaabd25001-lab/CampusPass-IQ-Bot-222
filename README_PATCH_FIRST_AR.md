# اقرأ أولًا — باتچ v5.0.7

هذا باتچ إصلاحي يرفع فوق **v5.0.6**.

1. فك ضغط الملف.
2. ارفع الملفات الموجودة داخله إلى جذر GitHub.
3. وافق على استبدال الملفات القديمة.
4. لا ترفع ملف ZIP وحده.
5. انتظر Railway حتى يصبح آخر Deployment بحالة `Active`.
6. أرسل `/cancel` ثم جرّب إنشاء عرض جديد.

لا تغيّر `DATABASE_URL` أو `ENCRYPTION_KEY` أو `BOT_TOKEN`.
