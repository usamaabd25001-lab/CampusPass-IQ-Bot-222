# إعادة البناء الهندسية - المرحلة الأولى

هذه المرحلة تؤسس انتقالًا آمنًا دون تعطيل ميزات V11.7.4.

## المنفذ

- سياسة مركزية حتمية لألوان أزرار Telegram الأصلية.
- إزالة التضارب الذي يسمح لزر خطر بالظهور كلون نجاح.
- دعم تخصيص دقيق عبر `BUTTON_STYLE_OVERRIDES_JSON`.
- Feature flag باسم `FEATURE_CENTRAL_BUTTON_STYLES` للرجوع الفوري.
- خفض توليد التقارير الثقيلة إلى عملية PDF واحدة متزامنة افتراضيًا لحماية Render Free.
- اختبارات مستقلة لسياسة الألوان.

## مثال إعداد الألوان

```json
{
  "exact": {
    "favorites:list": "primary",
    "back_to_main": "primary"
  },
  "prefixes": {
    "receipt:approve": "success",
    "receipt:reject": "danger",
    "nav:": "default"
  }
}
```

يوضع JSON في متغير Render باسم `BUTTON_STYLE_OVERRIDES_JSON`.

## القاعدة

- `primary`: فتح، انتقال رئيسي، عرض.
- `success`: حفظ، قبول، تفعيل، شراء، نشر.
- `danger`: حذف، رفض، إلغاء نهائي، حظر.
- `default`: رجوع، تفاصيل، تصفية، إجراءات محايدة.

## المراحل التالية

1. إضافة جدول overrides ولوحة مالك لتعديل كل زر دون Render env.
2. نقل إنشاء المنصة إلى Web App المالك.
3. نقل إنشاء وإدارة العرض إلى Web App مع الحفاظ على منطق V11.7.4.
4. استبدال التقارير القديمة بخطط Free/Plus/Pro وJobs دائمة.
5. تعطيل المسارات القديمة خلف Feature Flags ثم حذفها بعد الاختبارات.
