# CampusPass IQ — R4 Provider Commerce Web App

الإصدار: `11.8.0-provider-commerce-webapp`

## ما تغير

هذه المرحلة تعيد بناء مساري إدارة تجاريين عاليي الحساسية دون تغيير واجهة الطالب:

1. إضافة منصة من لوحة المالك عبر Telegram Web App متسلسل.
2. إضافة عرض من لوحة المنصة عبر Telegram Web App متسلسل ومتفرع.
3. تقوية عقد زر الرجوع بحيث يعني «خطوة واحدة للخلف» ولا يعني «الرئيسية».

## إضافة المنصة

المعالج الجديد يدعم بالترتيب: الاسم العربي الاختياري مع تحقق عربي صارم، الاسم الإنجليزي الاختياري مع تحقق إنجليزي، Telegram ID إلزامي للمالك، وصف اختياري، موظفون اختياريون بـ ID أو @username، نسبة إدارة اختيارية 0–100، أجور CampusPass ثابتة لكل طلب، وشعار اختياري مع معاينة. الـ slug داخلي ولا يطلب من المستخدم. المنصة تُنشأ Active مباشرة، ولا يشترط الشعار لإنشاء العرض أو لتفعيل المنصة.

تمت إضافة `default_service_fee_iqd` إلى `cp_providers`. كل عرض جديد عبر Web App يأخذ Snapshot من هذه القيمة في `cp_offers.service_fee_iqd` حتى لا يؤدي تغيير العمولة مستقبلًا إلى إعادة تفسير العروض القديمة.

## إضافة العرض

المعالج الجديد يحتفظ بمنطق الكتالوج والتسعير والصلاحية والمخزون والضمان الموجود مسبقًا، لكنه يستبدل الأسئلة المتناثرة بواجهة متسلسلة:

القسم → الخدمة → الفئة/المدة → السعر → طريقة الحصول على الخدمة → نوع الحساب عند الحاجة → إعداد التفعيل عند الحاجة → الحدود والضمان → شرح العرض وGemini → تعليمات التسجيل والتفعيل → المعاينة.

الخطوات متفرعة. مثال: عرض كود تفعيل لا يعرض إعداد حساب مشترك، وعرض OTP يعرض مزود البريد وإعدادات الحساب. يتم حفظ Draft الواجهة في `localStorage` ويرجع زر Back إلى آخر خطوة شاهدها المستخدم فعليًا.

## السعر العراقي

كل الحقول المالية الجديدة تقبل الأرقام العربية والفارسية والإنجليزية. عند كتابة رقم أقل من 250 مثل `10` لا يحوله النظام سرًا؛ تظهر مطالبة تأكيد `10,000 د.ع`. بعد التأكيد فقط يرسل المبلغ الكامل إلى السيرفر.

## الذكاء الاصطناعي

Gemini يستخدم ككاتب شرح فقط. يرسل له النظام الحقائق التي أدخلها الموظف: القسم، الخدمة، الفئة، السعر، المدة، طريقة التفعيل، والضمان. البرومبت يمنعه من اختراع سعر أو ميزة أو مدة أو ضمان. النص الناتج يوضع داخل textarea ويمكن تعديله أو استرجاع النص السابق.

## الأمان والتدقيق

- النقل عبر `fetch()` وليس Telegram `sendData()`.
- كل طلب Web App يحمل `X-Telegram-Init-Data`.
- السيرفر يتحقق من Telegram initData بتوقيع BOT_TOKEN ثم يتحقق من صلاحية الموظف على provider_id.
- Pydantic + Service validation يعيدان تدقيق البيانات حتى عند تجاوز JavaScript يدويًا.
- رفع شعار المنصة يمر عبر فحص الحجم/الصيغة/moderation ثم يحفظ Telegram file_id بدل تخزين الملف الخام في قاعدة البيانات.
- المخزون وOTP والضمان تستفيد من خدمات المشروع القائمة بدل إنشاء منطق موازٍ.
- سؤال الضمان في إنشاء العرض إجابة صريحة إجبارية (نعم/لا)، ويُرفض الطلب على السيرفر إذا لم تُحدد الحالة.

## تنظيم لوحة المنصة

- `📥 بريد الطلبات والإثباتات` أصبح `📥 مركز المعالجة`.
- `💰 أرباح المنصة وسحب الرصيد` أصبح `💰 الأرباح والحسابات` ويعرض سجلًا ماليًا مفيدًا حتى في نموذج الدفع المباشر.
- إدارة البريد/OTP والمخزون انتقلت إلى داخل `🛍 متجري والعروض` بدل ازدحام لوحة المنصة الرئيسية.
- زر الشعار يعرض الشعار الحالي إن وجد، ويظهر `🟢 تغيير الصورة` تحته.
- طرق الدفع الموجودة أصلًا تستخدم التسميات الواضحة: دفع إلكتروني (ماستر كارد، زين كاش) ودفع/تحويل رصيد (آسيا، زين، كورك).

## Build Gates

Docker يشغل قبل النشر:

- compileall
- AI support integration validation
- import architecture validation
- Web App architecture contract
- navigation contract
- UI public API validation
- Render build/runtime verification

`validate_navigation_contract.py` يمنع تحويل زر مكتوب عليه «رجوع» إلى `back_to_main` في الأزرار الحرفية، ويتحقق من وجود history حقيقي وTelegram BackButton داخل Web Apps الجديدة.

## التوافق

مسارات FSM القديمة لم تُحذف في هذه المرحلة؛ تبقى fallback إذا لم يوجد PUBLIC_BASE_URL. المسارات الجديدة هي الافتراضية على Render عندما يكون عنوان الخدمة العام متاحًا. هذا يسمح بالتحويل التدريجي ثم حذف المسارات القديمة بعد اختبار الإنتاج.
