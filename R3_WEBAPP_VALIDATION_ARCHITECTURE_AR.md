# R3 — Web App Validation Architecture

هذه المرحلة تنقل واجهات الإدخال الرئيسية إلى Telegram Web App مع إبقاء منطق الأعمال القديم كمسار احتياطي حتى يثبت البديل الجديد في الإنتاج.

## الواجهات المشمولة

1. **معلوماتي**: `/webapp/student/profile`
2. **إضافة منصة من لوحة المالك**: `/webapp/admin/provider`
3. **إضافة عرض من لوحة المنصة**: `/webapp/provider/offer`

## عقد الأمان

- لا تستخدم الواجهات `Telegram.WebApp.sendData`.
- كل طلب API يرسل `window.Telegram.WebApp.initData` في `X-Telegram-Init-Data`.
- FastAPI يعيد التحقق من توقيع Telegram ووقت `auth_date` باستخدام `BOT_TOKEN`.
- لا يعتمد السيرفر على `provider_id` القادم من المتصفح وحده؛ تتم إعادة مطابقة المستخدم مع صلاحيات المنصة في قاعدة البيانات.
- Pydantic والخدمات يعيدان التحقق من الحقول حتى لو تم تجاوز JavaScript يدويًا.
- الحفظ يتم داخل transaction قاعدة البيانات، ثم رسالة Telegram التأكيدية تكون best-effort حتى لا يتحول فشل إرسال الرسالة إلى فشل كاذب لعملية حفظ نجحت.

## التحقق في الواجهة

- زر **التالي/الحفظ معطل افتراضيًا**.
- لا يمكن الانتقال قبل اكتمال وصحة كل الحقول الإلزامية للخطوة الحالية.
- الأخطاء تظهر أسفل الحقل مباشرة ويتغير إطار الحقل فورًا.
- BackButton الأصلي من Telegram يعيد خطوة مع الحفاظ على البيانات.
- نموذج العرض يستخدم `sessionStorage` لحفظ المسودة أثناء التنقل وإعادة فتح الصفحة في نفس الجلسة.
- الحقول النصية المخصصة للعربية تحمل `lang="ar"` و`spellcheck="true"` للاستفادة من قاموس الجهاز مع قواعد السيرفر المحلية.

> ملاحظة: التدقيق الإملائي الكامل على مستوى قاموس لغوي ليس مضمونًا بواسطة Regex؛ المتصفح يوفر التدقيق الإملائي المحلي، بينما السيرفر يطبق قواعد اللغة/البنية، منع العبث، منع HTML والروابط حيث لا يسمح بها، ومنع التكرار غير الطبيعي. لا يستخدم Gemini لاتخاذ قرار قبول أو رفض البيانات.

## معلوماتي

الحقول الحالية فقط، دون إضافة بيانات جديدة:

- الاسم الثلاثي العربي — إلزامي.
- رقم الموبايل العراقي — إلزامي، ويخزن بصيغة `+964...`.
- المحافظة — إلزامية من القائمة الحالية.
- الجامعة/المعهد — إلزامي، نص مرن حتى لا تمنع جامعة جديدة.
- الكلية — إلزامية.
- القسم/الاختصاص — إلزامي.
- المرحلة — إلزامية.

الاسم يرفض الأرقام والرموز والعبارات الواضحة غير البشرية والتكرار العبثي، ولا يعتمد على قائمة مغلقة للأسماء العراقية.

## إضافة المنصة

- الاسم العربي — إلزامي.
- الاسم الإنجليزي — اختياري.
- Telegram ID لمالك المنصة — إلزامي وثابت؛ يجب أن يكون المالك قد بدأ البوت مسبقًا.
- وصف المنصة — اختياري.
- نسبة الإدارة — إلزامية ومن 0 إلى 100.
- `slug` يولد داخليًا ولا يظهر للمستخدم.
- المنصة تنشأ Pending/غير فعالة.
- يبقى رفع الشعار خطوة إلزامية عبر مسار البوت الحالي قبل التفعيل.
- يتم إنشاء ربط OWNER والاشتراك الافتراضي والأقسام الافتراضية وسجل Audit كما في منطق المشروع.

## إضافة العرض

واجهة Wizard تحفظ نفس قواعد المشروع الحالية:

- اختيار القسم والخدمة إلزامي، مع إضافة/تعديل الاسم من الواجهة.
- اسم العرض والسعر إلزاميان.
- السعر المؤقت يفرض سعر خصم ونهاية مستقبلية.
- طريقة التسليم والتفعيل إلزامية.
- نوع الصلاحية والحقول المشروطة التابعة له إلزامية.
- وقت بداية الاشتراك إلزامي.
- الحد اليومي إما رقم صحيح أو غير محدود.
- الوصف والشروط حسب المنطق القديم.
- Gemini زر اختياري لاقتراح الوصف فقط؛ لا يدخل في التحقق.
- العرض يحفظ **Draft** ولا ينشر مباشرة.
- دليل التسجيل والتفعيل القديم يبقى إلزاميًا، ويكمله صاحب المنصة من رسالة البوت بعد حفظ المسودة.

## Build Gate

أضيف `scripts/validate_webapp_contract.py` إلى Docker build. يفشل النشر إذا:

- اختفت إحدى صفحات Web App المطلوبة.
- عاد استخدام `sendData`.
- اختفى `X-Telegram-Init-Data` أو Fetch أو BackButton.
- اختفى تعطيل زر Next أو `spellcheck`.
- اختفت مسارات FastAPI/Pydantic أو تحقق initData.
- أزيلت رسالة البوت بعد الحفظ.
- حُذف مسار الـFSM الاحتياطي قبل اكتمال الانتقال.

لا توجد Migration جديدة في R3؛ تمت إعادة استخدام جداول المنصات والعروض والملف الشخصي الموجودة.
