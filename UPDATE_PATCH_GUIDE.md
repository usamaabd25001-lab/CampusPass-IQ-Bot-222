# دليل Patch من V3.2.0 إلى V4.0.0

هذا الملف مخصص لصاحب المشروع الذي لا يريد تعديل Python يدويًا.

## قبل الرفع

1. من Railway افتح PostgreSQL وأنشئ Backup أو Snapshot قابلًا للاسترجاع.
2. نزّل نسخة من مستودع GitHub الحالي.
3. سجّل قيمة `ENCRYPTION_KEY` الحالية في مكان آمن، ولا تغيرها.
4. فعّل `MAINTENANCE_MODE=true` أثناء الترقية إذا كان البوت يستقبل طلبات حقيقية.

## تطبيق Patch

1. فك `campuspass-patch-v3.2.0-to-v4.0.0.zip`.
2. ارفع الملفات والمجلدات الموجودة داخله إلى جذر المستودع.
3. وافق على استبدال الملفات المطابقة.
4. إذا ذكر `PATCH_MANIFEST.json` ملفًا محذوفًا، احذفه من المستودع.
5. حدّث Railway Variables بالقيم الجديدة من `RAILWAY_VARIABLES.txt`.
6. اعمل Commit واحدًا واضحًا، مثل `Upgrade CampusPass to V4.0.0`.

## أثناء النشر

الترتيب داخل التطبيق:

```text
إنشاء الجداول الجديدة
→ تشغيل migrations المسجلة
→ تحويل العروض القديمة إلى الكتالوج
→ إنشاء سياسات المدة وWorkflows
→ Seed الإعدادات
→ Commit
→ تشغيل Scheduler وTelegram وAPI
```

لا يحذف أي مستخدم أو طلب أو منصة أو عرض قديم.

## الفحص بعد النشر

- Railway Deployment: الحالة Success.
- `/health`: يعيد `ok` أو `warning` مفهومًا، وليس خطأ قاعدة بيانات.
- لوحة الإدارة ← صحة النظام.
- `/start` والتسجيل.
- فتح الاشتراكات ثم منصة وقسم وخدمة وعرض.
- إنشاء طلب تجريبي ورفعه وقبوله وتسليمه.
- فتح `اشتراكاتي` والوصل.

بعد نجاح الفحص، أعد `MAINTENANCE_MODE=false`.

## الرجوع عند الفشل

1. أعد `MAINTENANCE_MODE=true`.
2. Revert للـCommit أو Redeploy للإصدار السابق.
3. لا تحذف PostgreSQL.
4. إذا كان الخطأ بعد Migration، استخدم Backup فقط بعد التأكد أن الرجوع البرمجي لا يكفي.

جداول V4 الجديدة لا تزعج V3.2 غالبًا، لكن V3.2 لن يفهم ميزات الكتالوج والاشتراكات الجديدة. لا تستقبل طلبات جديدة على V4 ثم ترجع إلى V3.2 إلا في حالة طوارئ وبمراجعة البيانات.
